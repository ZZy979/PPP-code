#include <iostream>

using namespace std;

// Read numbers in decimal, hexadecimal, and octal form.
int main() {
    int a, b, c, d;
    cin >> a >> hex >> b >> oct >> c >> d;
    cout << a << ' ' << b << ' ' << c << ' ' << d << '\n';
    return 0;
}
